# ── Python ──────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*$py.class
.venv/
venv/
*.egg-info/
.mypy_cache/
.pytest_cache/
.ruff_cache/

# ── Secrets & Credentials (CRITICAL — never commit) ─────────────
.env
.env.*
!.env.example
credentials/
secrets/
*service-account*.json
*-sa.json
application_default_credentials.json

# ── Generated / Local-only ───────────────────────────────────────
chroma_db/
app/data/daily_reports.json
app/data/daily_reports_*.json
# SQLite metadata DB (akun & tenant — berisi hash password, JANGAN commit)
*.db
app/data/*.db
*.pyc
*.pyo

# ── Node.js ──────────────────────────────────────────────────────
node_modules/
npm-debug.log*

# ── IDE ──────────────────────────────────────────────────────────
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db

# ── Laporan / Build artifacts (not needed in repo) ───────────────
laporan/node_modules/
laporan/mis_unpacked/
laporan/*.png
laporan/*.docx

# ── Logs ─────────────────────────────────────────────────────────
*.log
logs/

# ── Local-only / not in submission zip ──────────────────────────
# CLAUDE.md is generated by /init for assistant context; not part of source.
CLAUDE.md
# Decoded mockup assets from design hand-off — reference only.
_decoded_assets/
_decoded_mobile.html

# ── Flutter build artifacts (mobile/) ───────────────────────────
mobile/.dart_tool/
mobile/build/
mobile/.flutter-plugins
mobile/.flutter-plugins-dependencies
mobile/.packages
mobile/pubspec.lock

# ── Handover doc (berisi credential/infra — JANGAN commit ke repo publik) ──
# Baris HANDOVER.md pernah ada di commit 5943661 tapi TIDAK ikut ter-squash saat
# PR #25 di-merge, jadi ia hilang dari main. Salinan kanonik file itu sekarang
# hidup di folder induk Fortunas/ (di luar work tree) — pola ini disengaja:
# file di luar repo tidak bisa ikut ter-commit walau .gitignore salah lagi.
HANDOVER.txt
HANDOVER.md

# ── Submission archive itself ────────────────────────────────────
fortunas-ai.zip
# Claude Code local (preview launch configs)
.claude/

# Uploaded product images (runtime, not versioned)
app/data/product_images/
